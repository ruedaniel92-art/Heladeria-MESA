{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Backend: restart clean",
			"type": "shell",
			"command": "powershell",
			"args": [
				"-NoProfile",
				"-ExecutionPolicy",
				"Bypass",
				"-Command",
				"$listeners = Get-NetTCPConnection -LocalPort 3000 -State Listen -ErrorAction SilentlyContinue | Select-Object -ExpandProperty OwningProcess -Unique; if ($listeners) { $listeners | ForEach-Object { Stop-Process -Id $_ -Force -ErrorAction SilentlyContinue } }; npm start"
			],
			"options": {
				"cwd": "${workspaceFolder}"
			},
			"isBackground": true,
			"group": "build"
		}
	]
}